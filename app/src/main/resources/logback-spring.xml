<?xml version="1.0" encoding="UTF-8"?>
<!--
  Sends log output to the error stream when a command line is running, and leaves it on the output
  stream everywhere else.

  A command's result goes to the output stream, and a run piped into another program has to arrive
  as that result alone. Logging shares that stream by default, so Spring's own debug switch puts
  several kilobytes of framework report in front of the answer, and a failure lands in the middle
  of it.

  Split by profile rather than changed outright, because only a one shot command has a caller
  reading its output. The desktop has no pipe, so moving its logging would gain nothing and change
  where anyone already looks for it.
-->
<configuration>

  <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

  <springProfile name="cli">
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
      <target>System.err</target>
      <encoder>
        <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        <charset>${CONSOLE_LOG_CHARSET}</charset>
      </encoder>
    </appender>
    <root level="INFO">
      <appender-ref ref="CONSOLE"/>
    </root>
  </springProfile>

  <springProfile name="!cli">
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
    <root level="INFO">
      <appender-ref ref="CONSOLE"/>
    </root>
  </springProfile>

</configuration>
